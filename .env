# --- Database ---
# Any SQLAlchemy-compatible URL, e.g.:
# postgresql+psycopg2://user:password@host:5432/dbname
# mysql+pymysql://user:password@host:3306/dbname
# sqlite:///local.db
#
# This matches the same example Postgres DB used in the original GW repo
# (see docker-compose.yml to spin up a local instance with these creds).
DATABASE_URL=postgresql+psycopg2://export_user:export_pass@localhost:5433/export_db

# --- SFTP delivery ---
# Local test SFTP server (docker-compose "sftp" service, atmoz/sftp).
SFTP_HOST=localhost
SFTP_PORT=2222
SFTP_USERNAME=sftp_user
# Use EITHER a password OR a private key path (key takes priority if set)
SFTP_PASSWORD=sftp_pass
SFTP_PRIVATE_KEY_PATH=
# Remote directory exported files get uploaded into
SFTP_REMOTE_DIR=/incoming

# --- Email delivery ---
# Gmail SMTP. SMTP_USERNAME must be a full @gmail.com address, and
# SMTP_PASSWORD must be a 16-character Google App Password (NOT your regular
# Gmail password) — generate one at https://myaccount.google.com/apppasswords
# (requires 2-Step Verification to be enabled on the account first).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=@gmail.com
SMTP_PASSWORD=
EMAIL_FROM=@gmail.com

# Local scratch folder where files are written before upload
LOCAL_OUTPUT_DIR=./output

# --- Logging / tracking ---
LOG_LEVEL=INFO
LOG_DIR=./logs
